{
  "name": "argo-checkout",
  "version": "0.0.0",
  "private": true,
  "repository": "https://github.com/lemonmade/remote-ui.git",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "sk test",
    "lint": "sk lint",
    "type-check": "sk type-check",
    "build": "sk build; yarn run build:argo",
    "version-bump": "lerna version --no-push --include-merged-tags",
    "deploy": "lerna publish from-package --yes",
    "build:argo": "lerna run build",
    "server:argo": "yarn workspace @shopify/argo-admin-playground run server",
    "simulator:argo": "yarn workspace @shopify/argo-admin-cli run server"
  },
  "devDependencies": {
    "@sewing-kit/cli": "^0.2.0",
    "@sewing-kit/config": "^0.1.0",
    "@sewing-kit/eslint-plugin": "^0.0.14",
    "@sewing-kit/plugin-eslint": "^0.1.0",
    "@sewing-kit/plugin-javascript": "^0.2.0",
    "@sewing-kit/plugin-jest": "^0.1.0",
    "@sewing-kit/plugin-package-flexible-outputs": "^0.1.0",
    "@sewing-kit/plugin-react": "^0.1.0",
    "@sewing-kit/plugin-typescript": "^0.1.0",
    "@types/execa": "^0.9.0",
    "lerna": "^3.22.1"
  },
  "resolutions": {
    "@typescript-eslint/eslint-plugin": "^4.0.0",
    "@typescript-eslint/experimental-utils": "^4.0.0",
    "@typescript-eslint/parser": "^4.0.0",
    "typescript": "^4.1.0"
  }
}
