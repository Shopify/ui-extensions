---
'@shopify/ui-extensions': minor
---

Add types for the POS validation resolution targets (`pos.cart.validations.resolution.render`, `pos.payment.validations.resolution.render`) and the target-scoped `shopify.resolution` API.
