---
'@shopify/ui-extensions': minor
---

Add the `s-pos-list` web component for POS. Supply rows as plain data and `itemTemplates` as a string of `<s-pos-list-item>` markup parsed and compiled by POS. Templates support row-field bindings and conditional content.
