import { createRemoteComponent } from '@remote-ui/core';

/**
 * Buttons are used primarily for actions, such as “Add”, “Close”, “Cancel”, or “Save”.
 */
export var Button = createRemoteComponent('Button');