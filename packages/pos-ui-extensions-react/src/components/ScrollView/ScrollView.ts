import {createRemoteReactComponent} from '@remote-ui/react';
import {ScrollView as BaseScrollView} from '@shopify/pos-ui-extensions';

export const ScrollView = createRemoteReactComponent(BaseScrollView);
