import {LayoutSection as BaseLayoutSection} from '@shopify/admin-ui-extensions';
import {createRemoteReactComponent} from '@remote-ui/react';

export const LayoutSection = createRemoteReactComponent(BaseLayoutSection);
