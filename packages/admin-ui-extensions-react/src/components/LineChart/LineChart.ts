import {LineChart as BaseLineChart} from '@shopify/admin-ui-extensions';
import {createRemoteReactComponent} from '@remote-ui/react';

export const LineChart = createRemoteReactComponent(BaseLineChart);
