import React from 'react';
import {Spinner as PolarisSpinner} from '@shopify/polaris';

import {SpinnerProps} from '../../../client/core';

export default function Spinner(_: SpinnerProps) {
  return <PolarisSpinner />;
}
