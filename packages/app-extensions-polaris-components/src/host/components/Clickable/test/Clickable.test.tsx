// import '@shopify/react-testing/matchers';
import React from 'react';

import {mountWithAppProvider as mount} from '../../../../../../../test-utils/mount';
import {ClickableProps} from '../../../../client/core/components/Clickable';
import Clickable from '../Clickable';

describe('<Clickable />', () => {
  it('triggers the onAction prop when clicked', () => {});

  it('does not trigger the onAction prop of a parent Clickable component when clicked', () => {});
});
