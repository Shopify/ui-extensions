import type {SharedReferenceEntityTemplateSchema} from '../docs-type';

const data: SharedReferenceEntityTemplateSchema = {
  name: 'Modal',
  description:
    'The `Modal` component displays content in an overlay. Use to create a distraction-free experience such as a confirmation dialog or a settings panel.\n\nThe Modal component automatically determines whether to display inside your app or as a fullscreen overlay, removing the need for separate App Bridge API control.',
  category: 'Polaris web components',
  subCategory: 'Overlays',
  related: [],
};

export default data;
