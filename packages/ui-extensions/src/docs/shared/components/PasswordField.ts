import type {SharedReferenceEntityTemplateSchema} from '../docs-type';

const data: SharedReferenceEntityTemplateSchema = {
  name: 'PasswordField',
  description:
    'The `PasswordField` component securely collects sensitive password information from users. Use `PasswordField` for password entry to automatically mask input characters and provide a toggle to show or hide the password.' +
    '\n\nPassword fields support validation, help text, and accessibility features to create secure and user-friendly authentication experiences. For general text input, use [`TextField`](/docs/api/{API_NAME}/{API_VERSION}/polaris-web-components/forms/textfield).',
  category: 'Polaris web components',
  subCategory: 'Forms',
  related: [],
};

export default data;
