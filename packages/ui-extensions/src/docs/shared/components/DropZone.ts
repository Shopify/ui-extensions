import type {SharedReferenceEntityTemplateSchema} from '../docs-type';

const data: SharedReferenceEntityTemplateSchema = {
  name: 'DropZone',
  description:
    'The `DropZone` component lets users upload files through drag-and-drop or by clicking to browse. Use the `accept` property to restrict file types and listen for `change` and `droprejected` events to handle uploads and validation.',
  category: 'Polaris web components',
  subCategory: 'Forms',
  related: [],
};

export default data;
