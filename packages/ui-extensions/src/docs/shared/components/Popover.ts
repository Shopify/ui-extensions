import type {SharedReferenceEntityTemplateSchema} from '../docs-type';

const data: SharedReferenceEntityTemplateSchema = {
  name: 'Popover',
  description:
    'The popover component displays contextual content in an overlay triggered by a button using the `commandFor` attribute. Use for secondary actions, settings, or information that doesn\'t require a full modal.\n\nFor interactions that need more space or user focus, such as confirmations or complex forms, use [Modal](/docs/api/app-home/polaris-web-components/modal) instead.',
  category: 'Polaris web components',
  subCategory: 'Overlays',
  related: [],
};

export default data;
