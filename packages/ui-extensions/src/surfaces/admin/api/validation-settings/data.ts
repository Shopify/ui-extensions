export interface Data {
  validation: {
    /**
     * the validation's gid when active in a shop
     */
    id?: string;
    /**
     * the validation's unique identifier
     */
    uuid: string;
  };
}
