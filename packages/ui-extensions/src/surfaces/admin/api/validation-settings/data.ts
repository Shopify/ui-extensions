export interface Data {
  validation?: {
    /**
     * the Validation's gid when active in a shop.
     */
    id: string;
  };
  shopifyFunction: {
    /**
     * the Validation's unique id.
     */
    id: string;
  };
}
