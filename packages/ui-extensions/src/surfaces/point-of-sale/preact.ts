// This file serves as an entry point for the point-of-sale preact components
// The component type declarations are discovered through TypeScript's module resolution
// When the package is built, the components/*.d.ts files provide the JSX definitions

export {};
