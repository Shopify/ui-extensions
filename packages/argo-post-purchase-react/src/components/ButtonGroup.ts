import {ButtonGroup as BaseButtonGroup} from '@shopify/argo-post-purchase';
import {createRemoteReactComponent} from '@remote-ui/react';

export const ButtonGroup = createRemoteReactComponent(BaseButtonGroup);
