import {Separator as BaseSeparator} from '@shopify/argo-post-purchase';
import {createRemoteReactComponent} from '@remote-ui/react';

export const Separator = createRemoteReactComponent(BaseSeparator);
