import {HiddenForAccessibility} from '@shopify/argo-checkout';
import {createRemoteVueComponent} from '@remote-ui/vue';

export const ArgoHiddenForAccessibility = createRemoteVueComponent(HiddenForAccessibility);
