import {createRemoteReactComponent} from '@remote-ui/react';
import {Navigator as BaseNavigator} from '@shopify/retail-ui-extensions';

export const Screen = createRemoteReactComponent(BaseNavigator);
