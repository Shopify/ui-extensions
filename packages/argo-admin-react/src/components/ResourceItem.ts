import {ResourceItem as BaseResourceItem} from '@shopify/argo-admin';
import {createRemoteReactComponent} from '@remote-ui/react';

export const ResourceItem = createRemoteReactComponent(BaseResourceItem);
