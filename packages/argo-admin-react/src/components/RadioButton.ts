import {RadioButton as BaseRadioButton} from '@shopify/argo-admin';

import {createRemoteReactComponent} from '@remote-ui/react';

export const RadioButton = createRemoteReactComponent(BaseRadioButton);
