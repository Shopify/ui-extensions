import {TextContainer as BaseTextContainer} from '@shopify/argo-checkout';
import {createRemoteReactComponent} from '@remote-ui/react';

export const TextContainer = createRemoteReactComponent(BaseTextContainer);
