import {SkeletonText as BaseSkeletonText} from '@shopify/argo-checkout';
import {createRemoteReactComponent} from '@remote-ui/react';

export const SkeletonText = createRemoteReactComponent(BaseSkeletonText);
