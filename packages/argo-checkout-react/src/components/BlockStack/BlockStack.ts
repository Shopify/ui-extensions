import {BlockStack as BaseBlockStack} from '@shopify/argo-checkout';
import {createRemoteReactComponent} from '@remote-ui/react';

export const BlockStack = createRemoteReactComponent(BaseBlockStack);
